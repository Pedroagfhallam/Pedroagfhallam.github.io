{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Chrome against local HTML file",
            "url": "file://${workspaceFolder}/index.html",  // Adjust to the path of your HTML file
            "webRoot": "${workspaceFolder}"
        }
    ]
}
